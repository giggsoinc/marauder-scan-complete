# PatronAI — Branding Assets

Drop the brand PNGs here so the README and Streamlit dashboard can render them.

| File | Purpose |
|---|---|
| `patronai-logo.png` | Full wordmark (shield + "PatronAI") — README header, home page |
| `patronai-icon.png` | Shield glyph only — browser tab favicon, sidebar, app icon |

These files are in `.gitignore` (binary assets). Keep originals in your design tool.
